# Contract

The Solidity contract lives at `contracts/BaseHabit.sol` and uses `pragma solidity ^0.8.24;`.

The contract records habits, check-ins, streak values, growth points, and referrals. It does not contain payment, pool, withdraw, ERC721, or NFT mint logic.

Configured contract address:

`0x96843Ae3aa313700e8339dFc8b2E7fAC75dB8160`
