# Errors

The UI displays friendly errors for missing contract configuration, wrong network, wallet detection failures, rejected transactions, failed transactions, and read failures.

The app includes client error boundaries in `app/error.tsx` and `app/global-error.tsx`.
