# Testing

Useful local commands:

```bash
npm run test:wallets
npm run typecheck
npm run build
```

`test:wallets` checks provider selection for OKX, MetaMask, mixed injected providers, EIP-6963 announce flows, and contract read normalization.
